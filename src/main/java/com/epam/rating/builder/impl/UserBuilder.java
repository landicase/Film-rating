package com.epam.rating.builder.impl;

import com.epam.rating.builder.BuilderFactory;
import com.epam.rating.entity.user.User;

import java.sql.ResultSet;
import java.sql.SQLException;

public class UserBuilder {

    public User build(ResultSet resultSet) throws SQLException {
       // User user = new User();
       // buildUser(user, resultSet);
        // return user;
        return null;
    }

    void buildUser(User user, ResultSet resultSet) throws SQLException {
    }
}
